package medium

import java.util.*

fun customSortString(S: String, T: String): String {
    var sChars = S.toCharArray()
    var tChars = T.toCharArray()

//    tChars.sortedWith(kotlin.Comparator { o1, o2 ->
//    })

   return String(tChars)
}